import React from "react";

function ExperienceDetails() {
  return (
        <div className="mi-resume-details">
          <li>Identifies and resolves incidents/requests and logs all interactions</li>
          <li>Identifies and resolves incidents/requests and logs all interactions</li>
        </div>
  );
};

export default Resume;
